version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd weather-app-client
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: weather-app-client/dist
    files:
      - '**/*'
  cache:
    paths:
      - weather-app-client/node_modules/**/*